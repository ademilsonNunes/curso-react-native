import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';

const HelloWorldApp = () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}>
      <Text>Amanda Claudino</Text>                                   
    </SafeAreaView>
  )
}
export default HelloWorldApp;